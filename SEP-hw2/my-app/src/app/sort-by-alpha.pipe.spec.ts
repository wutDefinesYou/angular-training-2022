import { SortByAlphaPipe } from './sort-by-alpha.pipe';

describe('SortByAlphaPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByAlphaPipe();
    expect(pipe).toBeTruthy();
  });
});
